{{- if .Values.ingress.enabled }}
apiVersion: {{ include "formsflow.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ include "formsflow.names.fullname" . }}
  namespace: {{ include "formsflow.names.namespace" . | quote }}
  {{- $labels := include "formsflow.tplvalues.merge" ( dict "values" ( list .Values.ingress.labels .Values.commonLabels ) "context" . ) }}
  labels: {{- include "formsflow.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: forms-flow-documents-api
  {{- if or .Values.ingress.annotations .Values.commonAnnotations }}
  {{- $annotations := include "formsflow.tplvalues.merge" ( dict "values" ( list .Values.ingress.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "formsflow.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ingress.ingressClassName (eq "true" (include "formsflow.ingress.supportsIngressClassname" .)) }}
  ingressClassName: {{ .Values.ingress.ingressClassName | quote }}
  {{- end }}
  rules:
    {{- if .Values.ingress.hostname }}
    - host: {{ (tpl .Values.ingress.hostname .) | quote }}
      http:
        paths:
          {{- if .Values.ingress.extraPaths }}
          {{- toYaml .Values.ingress.extraPaths | nindent 10 }}
          {{- end }}
          - path: {{ include "formsflow.tplvalues.render" ( dict "value" .Values.ingress.path "context" $) }}
            {{- if eq "true" (include "formsflow.ingress.supportsPathType" .) }}
            pathType: {{ .Values.ingress.pathType }}
            {{- end }}
            backend: {{- include "formsflow.ingress.backend" (dict "serviceName" (include "formsflow.names.fullname" $) "servicePort" $.Values.ingress.servicePort "context" $) | nindent 14 }}
    {{- end }}
    {{- range .Values.ingress.extraHosts }}
    - host: {{ (tpl .name $) }}
      http:
        paths:
          - path: {{ default "/" .path }}
            {{- if eq "true" (include "formsflow.ingress.supportsPathType" $) }}
            pathType: {{ default "ImplementationSpecific" .pathType }}
            {{- end }}
            backend: {{- include "formsflow.ingress.backend" (dict "serviceName" (include "formsflow.names.fullname" $) "servicePort" $.Values.ingress.servicePort "context" $) | nindent 14 }}
    {{- end }}
    {{- if .Values.ingress.extraRules }}
    {{- include "formsflow.tplvalues.render" (dict "value" .Values.ingress.extraRules "context" $) | nindent 4 }}
    {{- end }}
  {{- if or (and .Values.ingress.tls (or (include "formsflow.ingress.certManagerRequest" ( dict "annotations" .Values.ingress.annotations )) .Values.ingress.selfSigned .Values.ingress.secrets )) .Values.ingress.extraTls }}
  tls:
  {{- if and .Values.ingress.tls (or (include "formsflow.ingress.certManagerRequest" ( dict "annotations" .Values.ingress.annotations )) .Values.ingress.secrets .Values.ingress.selfSigned) }}
    - hosts:
        - {{ (tpl .Values.ingress.hostname .) | quote }}
      secretName: {{ printf "%s-tls" (tpl .Values.ingress.hostname .) }}
    {{- end }}
    {{- if .Values.ingress.extraTls }}
    {{- include "formsflow.tplvalues.render" (dict "value" .Values.ingress.extraTls "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
{{- end }}